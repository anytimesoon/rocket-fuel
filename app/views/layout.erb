<!DOCTYPE html>
<html>
  <head>
  	<meta charset="utf-8" />

  	<!-- Set the viewport width to device width for mobile -->
  	<meta name="viewport" content="width=device-width" />
  	<title>Rocket Fuel - The best coffee around</title>

    <link href='../public/style.css' rel='stylesheet' type='text/css'>

  </head>

  <body>
    <div class="grid">
      <div class="col-6">
        <h1>Welcome to ROCKET FUEL!</h1>
        <h2>Get your coffee here</h2>
        <%if Helpers.is_logged_in?(session) %>
          <a href="/account"><%=Helpers.current_user(session).name%></a>
          <a href="/carts/<%=session[:cart_id]%>">Cart</a>
          <a href="/logout">Logout</a>
        <%else%>
          <a href="/login">Log in</a>
          <a href="/signup">Sign up</a>
        <%end%>
      </div>
    </div>

    <%= yield %>

      <div class="footer">
        <div class="grid">
          <div class="col-12">
            <div class="title-footer">
              <h4>Rocket Fuel</h4>

              <span>This is not a real shop. Don't buy anything here.</span>
            </div>
          </div>
        </div>
      </div>

    </div>
  </body>
</html>
